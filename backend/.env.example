# ============================================
# CONFIGURACIÓN DEL SERVIDOR BACKEND
# ============================================
# IMPORTANTE: Copia este archivo a .env y llena con tus valores reales
# .env está en .gitignore y NO debe subirse a Git

# DATABASE
# Connection string de Supabase PostgreSQL
# Formato: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
DATABASE_URL="postgresql://postgres:password@db.example.supabase.co:5432/postgres"

# SERVER
PORT=3001
NODE_ENV=development

# ============================================
# EXTERNAL APIS - UCN
# ============================================
# API de Hawaii (Mallas Curriculares)
HAWAII_API_URL=https://losvilos.ucn.cl/hawaii/api
HAWAII_AUTH_TOKEN=tu_token_aqui

# API de Puclaro (Avance Académico y Login)
PUCLARO_API_URL=https://puclaro.ucn.cl/eross/avance

# ============================================
# JWT (Para implementación futura)
# ============================================
# Para generar un secret seguro, ejecuta:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# JWT_SECRET=generar_con_el_comando_de_arriba
# JWT_EXPIRES_IN=24h

